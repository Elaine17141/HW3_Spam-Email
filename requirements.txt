# 基礎機器學習套件
scikit-learn>=1.0.2    # 機器學習模型、評估指標
numpy>=1.21.0          # 數值計算
pandas>=1.3.0          # 資料處理和分析
joblib>=1.1.0          # 模型持久化

# 資料下載和 HTTP 請求
requests>=2.26.0       # 下載資料集

# 可選但建議的套件
tqdm>=4.62.0          # 進度條（用於長時間運行的處理）
matplotlib>=3.4.0      # 用於繪製混淆矩陣和學習曲線（可選）
seaborn>=0.11.0       # 改善視覺化效果（可選）

# 開發工具（可選）
pytest>=6.2.5         # 單元測試
black>=21.12b0        # 程式碼格式化
flake8>=4.0.1         # 程式碼檢查